{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "wwm",
  "displayName": "Wicked Webflow MCP",
  "version": "0.2.0",
  "description": "Keep every client's Webflow MCP connection live at once, and load only the ones each project needs.",
  "author": { "name": "Wicked Apps", "url": "https://github.com/wickedapps" },
  "repository": "https://github.com/wickedapps/wicked-webflow-mcp",
  "license": "ISC",
  "keywords": ["webflow", "mcp", "agency", "multi-tenant", "oauth"],
  "userConfig": {
    "mcp_url": {
      "type": "string",
      "title": "Webflow MCP endpoint",
      "description": "Override only if Webflow changes the endpoint.",
      "default": "https://mcp.webflow.com/mcp"
    },
    "prefix": {
      "type": "string",
      "title": "Server name prefix",
      "description": "Prefix for every registered connection. Shorter = less context per tool name.",
      "default": "wf-"
    }
  }
}
