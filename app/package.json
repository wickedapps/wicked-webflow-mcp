{
  "name": "wicked-webflow-app",
  "private": true,
  "type": "module",
  "description": "Desktop front end for the wwm CLI. Never published — deliberately has no version field, so scripts/release.mjs has one fewer place to keep in step.",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "dev:vite": "vite",
    "build:vite": "tsc --noEmit && vite build",
    "check": "tsc --noEmit",
    "icons": "tauri icon src-tauri/icons/icon.png",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-dialog": "^2.7.2",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.2",
    "vite": "^6.0.3"
  }
}
